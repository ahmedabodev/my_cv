import 'package:flutter/material.dart';
import 'package:mywebsitr/portfolio/widget/video.dart';

class vediosscreen extends StatelessWidget {
  const vediosscreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          // VideoPlayerScreen(url:'images/todoprivacy.mp4'),
          // VideoPlayerScreen(url: 'images/apptesting.mp4'),
          // VideoPlayerScreen(url: 'images/todoprivacy.mp4'),
        ],
      ),
    );
  }
}
